# mcp-bridge 1.2.0

## 新功能
- 手动审批：文件修改、执行和截图逐次确认，默认开启此模式。
- 自动审批：常规已授权操作自动批准，命中风险规则的命令及截图仍确认。
- 完全访问：本机确认后开启全部权限，允许当前 Windows 用户权限范围内的工作目录外文件访问，无逐操作确认；退出时恢复之前的权限开关。不自动提权。
- GitHub 稳定版自动检查（启动后及每 6 小时），全局新版入口，可关闭自动检查。
- 手动下载、进度、取消及重试；SHA256、长度、文件头校验，安装前重新校验并本机确认。有运行任务或待审批操作时阻止安装。

## 修复与兼容
- 远程截图调用不能用附加 preConfirmed 参数跳过审批。
- 清理已结束审批的超时计时器。
- 原配置、加密凭据和文件历史位置不变；没有审批模式的旧配置默认手动审批。已确认的完全访问选择会跨重启保留。

## 升级说明
- 1.1.1 不具备自动检查能力，第一次升级需要手动下载安装包或 ZIP。
- ZIP 必须完整解压，不能仅复制 EXE；免安装版使用更新按钮会启动安装版向导。
- 安装包未进行开发者代码签名；不关闭系统安全保护。
- Execute 在所有模式下都不是操作系统沙箱；自动审批风险规则并不完备。完全访问仅适用于可信客户端。
- 本地构建不等于公开发布；后续新版需发布到 cnrpot/mcp-bridge 的稳定 GitHub Release，才会被检查发现。
